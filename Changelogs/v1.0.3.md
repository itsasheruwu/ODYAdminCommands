# v1.0.3

Fixes `/cleanlogs` so it cleans the actual server `logs` and `crash-reports` folders.

## Fixed

- Corrected server root resolution for `/cleanlogs`
- `/cleanlogs` now targets the Paper server root instead of the `plugins` directory
- `/cleanlogs` now uses ProtocolLib packet interception to avoid the Paper console command log when ProtocolLib is installed

## Changed

- README updated for the new `1.0.3` build artifact
